name: 01 - Building blocks

on: push

jobs:
    echo-sam:
        runs-on: ubuntu-latest
        steps:
            - name: hello world
              run: echo "hello there"
    echo-exit:
        runs-on: ubuntu-latest
        steps:
            - name: error step
              run: |
                echo "bye there"
                exit 1
            - name: say goodbye
              run: echo "goodbye"
